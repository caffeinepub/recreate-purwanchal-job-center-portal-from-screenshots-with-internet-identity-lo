## Recreate Purwanchal Job Center portal from screenshots with Internet Identity login, user profiles/onboarding, and admin CMS

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
